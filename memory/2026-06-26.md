# 2026-06-26

- Seeded `ops/maintenance/discord_targets.json` with the exact room list Andrea provided: `ocbuilder`, `Pine-screener`, `MT5`, `BITGET Trades`, and `TV Outcome Feedback Loop`.
- Confirmed the maintenance sweep does not compact live Discord thread context; it only checks Git repos and exported Discord context when configured.
- Updated `PROJECT_STATE.md` to note that live Discord context compaction is not something the current maintenance job can do in place.
- Cleaned obsolete cron jobs after Andrea flagged clutter in the Control UI: removed 11 disabled/old/test jobs and verified the scheduler now lists 5 active jobs. Kept active workspace maintenance, archive cleanup, monthly health, TV outcome weekly digest, and workspace Git sync. `Workspace Git Sync` has recent timeout errors and should be reviewed separately rather than removed blindly.
